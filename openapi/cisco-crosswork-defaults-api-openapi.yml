openapi: 3.2.0
info:
  description: Cisco Crosswork Workflow Manager (CWM) 2.1 offers RESTful APIs that enable automation engineers and developers to create, deploy, and manage workflows, adapters, jobs, resources, events, and workers within the Cisco CWM platform.
  title: Cisco Crosswork Workflow Manager Defaults API
  termsOfService: www.cisco.com
  contact:
    name: API Support
    url: https://www.cisco.com/support
  version: 2.1.0
  x-provenance:
    method: harvested
    authored_by: Cisco Crosswork
    harvested_by: API Evangelist
    harvested_on: '2026-08-19'
    first_party: true
    provider_published: true
    note: 'Reconstructed verbatim from the per-operation Swagger 2.0 JSON fragments Cisco serves for the Crosswork Workflow Manager API reference at developer.cisco.com/docs/crosswork/workflow-manager/. info, basePath and securityDefinitions are Cisco''s own meta block; each path/method object is Cisco''s own spec object, unmodified except that inline schemas carrying the swagger-ui $$ref marker were hoisted back into #/definitions and replaced with a real $ref.'
servers:
- url: /crosswork/cwm/v2
tags:
- name: Defaults
paths:
  /config/defaults:
    get:
      description: Retrieve all system default configurations.
      tags:
      - Defaults
      summary: List system defaults
      operationId: list_system_defaults
      responses:
        '200':
          description: System defaults retrieved successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.responseConfigurations'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                type: string
      __originalOperationId: list-system-defaults
      method: get
      path: /config/defaults
    patch:
      description: Update system default configurations partially.
      tags:
      - Defaults
      summary: Update system defaults
      operationId: update_system_defaults
      responses:
        '200':
          description: System defaults updated successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.responseConfigurations'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                type: string
      __originalOperationId: update-system-defaults
      method: patch
      path: /config/defaults
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/server.requestConfiguration'
        description: Partial configuration to apply.
        required: true
  /config/defaults/{key}:
    get:
      description: Retrieve a system default configuration by its key.
      tags:
      - Defaults
      summary: Get a system default by key
      operationId: get_system_default
      parameters:
      - description: Key name for the default configuration.
        name: key
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: System default retrieved successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                type: string
      __originalOperationId: get-system-default
      method: get
      path: /config/defaults/{key}
    put:
      description: Set or update a system default configuration by its key.
      tags:
      - Defaults
      summary: Update a system default by key
      operationId: update_system_default
      parameters:
      - description: Key name for the default configuration.
        name: key
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: System default updated successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        '400':
          description: Invalid request.
          content:
            application/json:
              schema:
                type: string
      __originalOperationId: update-system-default
      method: put
      path: /config/defaults/{key}
      requestBody:
        content:
          application/json:
            schema:
              type: string
        description: Value to assign to the default configuration.
        required: true
components:
  schemas:
    server.GetSystemDefaultResponse:
      type: object
      properties:
        description:
          type: string
        value:
          type: object
    server.responseConfigurations:
      type: object
      properties:
        actionTimeout:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        auditIncludeData:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        auditMaxDataSize:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        eventTimeout:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        jobRetentionPeriod:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        retryDelay:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        retryDelayMultiplier:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        retryJitter:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        retryMaxAttempts:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        retryMaxDelay:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        stateTimeout:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        systemActivityTimeout:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
        workflowTimeout:
          $ref: '#/components/schemas/server.GetSystemDefaultResponse'
    server.requestConfiguration:
      type: object
      properties:
        actionTimeout:
          type: integer
          default: 60
        auditIncludeData:
          type: boolean
          default: false
        auditMaxDataSize:
          type: integer
          default: 1024
        eventTimeout:
          type: integer
          default: 600
        jobRetentionPeriod:
          type: integer
          default: 30
        retryDelay:
          type: integer
          default: 10
        retryDelayMultiplier:
          type: number
          default: 1.5
        retryJitter:
          type: number
          default: 0.2
        retryMaxAttempts:
          type: integer
          default: 5
        retryMaxDelay:
          type: integer
          default: 300
        stateTimeout:
          type: integer
          default: 300
        systemActivityTimeout:
          type: integer
          default: 10
        workflowTimeout:
          type: integer
          default: 3600
  securitySchemes:
    Bearer:
      type: apiKey
      name: Authorization
      in: header
      description: 'JWT Authorization header using the Bearer scheme. Example: "Authorization: Bearer {token}"'
x-evidence:
- type: reference
  url: https://developer.cisco.com/docs/crosswork/workflow-manager/
- type: fragment-index
  url: https://pubhub.devnetcloud.com/media/crosswork-workflow-manager-api-document/docs/262737b2-b3c2-32cd-b07b-fdbdcd23918f/apis/
  count: 90
