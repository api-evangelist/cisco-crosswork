openapi: 3.2.0
info:
  description: Cisco Crosswork Workflow Manager (CWM) 2.1 offers RESTful APIs that enable automation engineers and developers to create, deploy, and manage workflows, adapters, jobs, resources, events, and workers within the Cisco CWM platform.
  title: Cisco Crosswork Workflow Manager Worker Profiles API
  termsOfService: www.cisco.com
  contact:
    name: API Support
    url: https://www.cisco.com/support
  version: 2.1.0
  x-provenance:
    method: harvested
    authored_by: Cisco Crosswork
    harvested_by: API Evangelist
    harvested_on: '2026-08-19'
    first_party: true
    provider_published: true
    note: 'Reconstructed verbatim from the per-operation Swagger 2.0 JSON fragments Cisco serves for the Crosswork Workflow Manager API reference at developer.cisco.com/docs/crosswork/workflow-manager/. info, basePath and securityDefinitions are Cisco''s own meta block; each path/method object is Cisco''s own spec object, unmodified except that inline schemas carrying the swagger-ui $$ref marker were hoisted back into #/definitions and replaced with a real $ref.'
servers:
- url: /crosswork/cwm/v2
tags:
- name: workerProfiles
paths:
  /workerProfile:
    get:
      description: Retrieve a list of all worker profiles defined in the system.
      tags:
      - workerProfiles
      summary: List all worker profiles
      operationId: list_worker_profiles
      responses:
        '200':
          description: Worker profiles retrieved successfully.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/server.WorkerProfile'
        '400':
          description: Bad request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.HTTPError'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.HTTPError'
      __originalOperationId: list-worker-profiles
      method: get
      path: /workerProfile
    post:
      description: Create a new worker profile with CPU and memory values to be assigned to workers.
      tags:
      - workerProfiles
      summary: Create a new worker profile
      operationId: create_worker_profile
      responses:
        '200':
          description: Worker profile created successfully.
          content:
            application/json:
              schema:
                type: string
        '400':
          description: Bad request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.HTTPError'
        '403':
          description: Forbidden.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.HTTPError'
      __originalOperationId: create-worker-profile
      method: post
      path: /workerProfile
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/server.WorkerProfile'
        description: Worker profile input details.
        required: true
  /workerProfile/{profileName}:
    delete:
      description: Delete a specific worker profile identified by its name.
      tags:
      - workerProfiles
      summary: Delete a worker profile
      operationId: delete_worker_profile
      parameters:
      - description: Profile name.
        name: profileName
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Worker profile deleted successfully.
          content:
            application/json:
              schema:
                type: string
        '400':
          description: Bad request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.HTTPError'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.HTTPError'
      __originalOperationId: delete-worker-profile
      method: delete
      path: /workerProfile/{profileName}
    get:
      description: Retrieve a specific worker profile by its name.
      tags:
      - workerProfiles
      summary: Get a specific worker profile
      operationId: get_worker_profile
      parameters:
      - description: Profile name.
        name: profileName
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Worker profile retrieved successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.WorkerProfile'
        '400':
          description: Bad request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.HTTPError'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.HTTPError'
      __originalOperationId: get-worker-profile
      method: get
      path: /workerProfile/{profileName}
    patch:
      description: Update an existing worker profile's CPU and memory settings. Updates will not apply to workers already created. To set CPUMax or MemMax to nil, provide an empty string.
      tags:
      - workerProfiles
      summary: Update a worker profile
      operationId: update_worker_profile
      parameters:
      - description: Profile name.
        name: profileName
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Worker profile updated successfully.
          content:
            application/json:
              schema:
                type: string
        '400':
          description: Bad request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.HTTPError'
        '403':
          description: Forbidden.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/server.HTTPError'
      __originalOperationId: update-worker-profile
      method: patch
      path: /workerProfile/{profileName}
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/server.WorkerProfileUpdate'
        description: Worker profile update payload.
        required: true
components:
  schemas:
    server.HTTPError:
      type: object
      properties:
        code:
          type: integer
          example: 400
        message:
          type: string
          example: status bad request
    server.WorkerProfile:
      type: object
      properties:
        cpuMax:
          type: string
        cpuMin:
          type: string
        memMax:
          type: string
        memMin:
          type: string
        profileName:
          type: string
    server.WorkerProfileUpdate:
      type: object
      properties:
        cpuMax:
          type: string
        cpuMin:
          type: string
        memMax:
          type: string
        memMin:
          type: string
  securitySchemes:
    Bearer:
      type: apiKey
      name: Authorization
      in: header
      description: 'JWT Authorization header using the Bearer scheme. Example: "Authorization: Bearer {token}"'
x-evidence:
- type: reference
  url: https://developer.cisco.com/docs/crosswork/workflow-manager/
- type: fragment-index
  url: https://pubhub.devnetcloud.com/media/crosswork-workflow-manager-api-document/docs/262737b2-b3c2-32cd-b07b-fdbdcd23918f/apis/
  count: 90
